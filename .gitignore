node_modules/
.next/
.env*.local
*.tsbuildinfo
.DS_Store

# Client dossiers: scraped third-party text, photos and contact data.
# Never commit — it is other companies data sitting in the template repo.
.context/

# Local Vercel CLI project link. Committed, it repoints every other
# working copy at whichever client was deployed last.
.vercel/
